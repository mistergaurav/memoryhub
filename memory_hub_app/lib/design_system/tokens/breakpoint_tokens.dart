/// Responsive breakpoint tokens
/// Use with MediaQuery to adjust layout based on screen size
class Breakpoints {
  Breakpoints._();

  /// Small devices (phones) - < 600px
  static const double sm = 600;

  /// Medium devices (tablets) - < 904px
  static const double md = 904;

  /// Large devices (desktop) - < 1232px
  static const double lg = 1232;

  /// Extra large devices (wide desktop) - >= 1232px
  static const double xl = 1232;
}
