# The Memory Hub - Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# JWT Secret Key - MUST be changed in production
# Generate a secure key using: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-change-in-production

# JWT Algorithm
ALGORITHM=HS256

# Token expiration settings
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days (default)
REFRESH_TOKEN_EXPIRE_DAYS=30

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MongoDB Connection URL
# For local development: mongodb://localhost:27017
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net
MONGODB_URL=mongodb://localhost:27017

# Database name
DB_NAME=memory_hub

# =============================================================================
# GOOGLE OAUTH SETTINGS (Optional)
# =============================================================================
# To enable Google Sign In, create OAuth credentials at:
# https://console.cloud.google.com/apis/credentials
# 
# Important: Add authorized redirect URIs:
# - For local: http://localhost:5000/api/v1/auth/google/callback
# - For Replit: https://your-repl-name.replit.dev/api/v1/auth/google/callback

# Google OAuth Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# FILE STORAGE SETTINGS
# =============================================================================

# Maximum file upload size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Allowed file extensions (comma-separated)
ALLOWED_FILE_EXTENSIONS=.jpg,.jpeg,.png,.gif,.pdf,.doc,.docx,.txt

# =============================================================================
# EMAIL SETTINGS (Optional - for email verification and notifications)
# =============================================================================

# SMTP Server Configuration (uncomment to enable)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-specific-password
# SMTP_FROM_EMAIL=noreply@memoryhub.com
# SMTP_FROM_NAME=The Memory Hub

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. Never commit the actual .env file to version control
# 2. Always use strong, unique values for SECRET_KEY in production
# 3. For Google OAuth to work, you MUST set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET
# 4. MongoDB must be running for the application to start
# 5. Keep your .env file secure and never share it publicly
#
# For Replit:
# - Use the Secrets tab to add environment variables securely
# - Secrets are automatically loaded as environment variables
# - Never hardcode sensitive data in your code